const fruits = ["maça", "banana", "laranja", "uva", "abacaxi"];

// printa todos os elementos do array
for (let i = 0; i < fruits.length; i++) { // length = tamanho do array
    console.log(fruits[i]);
}